services:
  - type: web
    name: slinnva-agenda
    env: node
    plan: starter
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: RESEND_API_KEY
        sync: false
      - key: CONTACT_EMAIL_FROM
        sync: false
      - key: NODE_ENV
        value: production
